<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bytessystem.com.dairymanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CustomerRegistration" />
        <activity android:name=".CustomerListView" />
        <activity android:name=".CustomerDetails" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddProduct" />
        <activity android:name=".ProductListView" />
        <activity android:name=".ProductDetails" />
        <activity android:name=".DailyBook" />
        <activity android:name=".AddCustomerProduct" />
        <activity android:name=".CustomerProductListView"></activity>
    </application>

</manifest>